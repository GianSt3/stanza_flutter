import 'package:flutter/material.dart';

class ChatPlayerWidget extends StatelessWidget {
  const ChatPlayerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
